# Scripts

| Script | Purpose |
|--------|---------|
| `check-secrets.sh` | Scan for accidental credential commits before push |

One-off DB/Sheets setup: `api/init-google-sheets.js`, `api/clear-and-reinit-sheets.js`.
